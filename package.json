{
  "license": "MIT",
  "scripts": {
    "start": "parcel ./src/*.html",
    "prebuild-dev": "shx rm -rf dist/*",
    "build-dev": "parcel build ./src/*.html --no-optimize",
    "prebuild": "shx rm -rf dist/*",
    "build": "parcel build ./src/*.html --public-url ./sg-budget-breakdown",
    "push-gh-pages": "push-dir --dir=dist --branch=gh-pages --cleanup --verbose"
  },
  "devDependencies": {
    "@types/react": "^18.0.27",
    "@types/react-dom": "^18.0.10",
    "parcel": "^2.8.3",
    "postcss": "^8.4.21",
    "process": "^0.11.10",
    "push-dir": "^0.4.1",
    "shx": "^0.3.4",
    "tailwindcss": "^3.2.4"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
