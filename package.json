{
  "license": "MIT",
  "scripts": {
    "start": "parcel ./src/*.html --public-url ./",
    "prebuild-dev": "shx rm -rf dist/*",
    "build-dev": "parcel build ./src/*.html --no-optimize --public-url ./",
    "prebuild": "shx rm -rf dist/*",
    "build": "parcel build ./src/*.html --public-url ./sg-budget-breakdown/",
    "push-gh-pages": "push-dir --dir=dist --branch=gh-pages --cleanup --verbose"
  },
  "devDependencies": {
    "parcel": "^2.8.0",
    "postcss": "^8.4.19",
    "push-dir": "^0.4.1",
    "shx": "^0.3.4",
    "tailwindcss": "^3.2.4"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.3"
  }
}
